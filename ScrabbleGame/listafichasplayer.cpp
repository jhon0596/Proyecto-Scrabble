#include "listafichasplayer.h"
#include "listafichas.h"
#include "node.h"
/*
listaFichasPlayer::listaFichasPlayer()
{
  head = nullptr;

}
void listaFichasPlayer::addF(string data, int valor) {

    int posicion[1][1] = {{-1}};
    node *temp = new node(valor, nullptr, data, posicion);
    temp->setDato(data);


    if (head == nullptr) {

        head = temp;



    } else {

        temp->setNext(head);
        head = temp;

    }
}*/

